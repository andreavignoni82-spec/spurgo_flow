// Test/emulator placeholders only. Never place real project credentials here.
export const firebaseTestConfig=Object.freeze({apiKey:'TEST_API_KEY',authDomain:'localhost',projectId:'spurgoflow-test',useEmulator:true});
