const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const options=(rows,value,label)=>rows.map(x=>`<option value="${esc(x.id)}" ${String(x.id)===String(value)?'selected':''}>${esc(label(x))}</option>`).join('');
export function renderInterventions(container,state){const {rows,filters,form,clients,operators,teams,vehicles,saving,error,errors={}}=state;container.innerHTML=`<div class="sf-interventions"><header><div><h1>Interventi</h1><p>Crea, assegna, modifica e gestisci gli interventi</p></div><button class="btn primary" data-action="new">+ Nuovo intervento</button></header>${error?`<div class="sf-interventions__error">${esc(error)}</div>`:''}<div class="sf-interventions__toolbar"><input type="date" data-filter="date" value="${esc(filters.date)}"><input data-filter="query" value="${esc(filters.query)}" placeholder="Cerca cliente, indirizzo, ID..."><select data-filter="status"><option value="">Tutti gli stati</option>${['Urgente','Programmato','In corso','Terminato','Annullato'].map(x=>`<option ${filters.status===x?'selected':''}>${x}</option>`).join('')}</select><select data-filter="operatorId"><option value="">Tutti gli operatori</option>${options(operators,filters.operatorId,x=>x.name||x.username)}</select></div><div class="sf-interventions__list">${rows.length?rows.map(i=>`<article data-intervention-id="${esc(i.id)}"><div><b>${esc(i.id)} · ${esc(i.clientName)}</b><small>${esc(i.date)} · ${esc(i.time)} · ${esc(i.address)}</small><small>${esc(i.type||'Intervento')} · ${esc(i.request)}</small><small>Operatori: ${esc(i.assignedOperatorIds.map(id=>operators.find(x=>String(x.id)===id)?.name).filter(Boolean).join(', ')||'Da assegnare')}</small></div><span>${esc(i.status)}</span><div><button data-action="edit">Modifica</button><button data-action="delete">Elimina</button></div></article>`).join(''):'<p>Nessun intervento per la data o i filtri selezionati.</p>'}</div>${form?`<div class="sf-interventions__modal"><form data-role="form"><h2>${form.id?'Modifica':'Nuovo'} intervento</h2><label>Cliente<select name="clientId"><option value="">Seleziona</option>${options(clients,form.clientId,x=>x.name)}</select><em>${esc(errors.clientId)}</em></label><label>Data<input name="date" type="date" value="${esc(form.date)}"><em>${esc(errors.date)}</em></label><label>Ora<input name="time" type="time" value="${esc(form.time||'08:00')}"></label><label>Tempo stimato<input name="estimatedMinutes" type="number" value="${esc(form.estimatedMinutes||60)}"></label><label class="wide">Indirizzo<input name="address" value="${esc(form.address)}"></label><label class="wide">Richiesta / attività<input name="request" value="${esc(form.request)}"></label><label>Stato<select name="status">${['Programmato','Urgente','In corso','Terminato','Annullato'].map(x=>`<option ${form.status===x?'selected':''}>${x}</option>`).join('')}</select></label><label>Priorità<select name="priority">${['Normale','Urgente'].map(x=>`<option ${form.priority===x?'selected':''}>${x}</option>`).join('')}</select></label><label>Squadra<select name="teamId"><option value="">Nessuna</option>${options(teams,form.teamId,x=>x.name)}</select></label><label>Mezzo<select name="vehicleId"><option value="">Nessuno</option>${options(vehicles,form.vehicleId,x=>x.name)}</select></label><fieldset class="wide"><legend>Operatori assegnati</legend>${operators.filter(x=>x.active!==false).map(x=>`<label><input type="checkbox" name="assignedOperatorIds" value="${esc(x.id)}" ${form.assignedOperatorIds?.map(String).includes(String(x.id))?'checked':''}>${esc(x.name||x.username)}</label>`).join('')}</fieldset><label class="wide">Note<textarea name="notes">${esc(form.notes)}</textarea></label><footer><button type="button" data-action="cancel">Annulla</button><button class="btn primary" ${saving?'disabled':''}>${saving?'Salvataggio…':'Salva intervento'}</button></footer></form></div>`:''}</div>`;}
