import { normalizeIntervention } from '../../shared/models/intervention.js';
export const todayKey = (now=new Date()) => `${now.getFullYear()}-${String(now.getMonth()+1).padStart(2,'0')}-${String(now.getDate()).padStart(2,'0')}`;
export function filterInterventions(rows=[], filters={}) { const q=String(filters.query??'').toLowerCase(); return rows.map(normalizeIntervention).filter(row => (!filters.date||row.date===filters.date)&&(!filters.status||row.status===filters.status)&&(!filters.operatorId||row.operatorId===filters.operatorId||row.assignedOperatorIds.includes(filters.operatorId))&&(!q||[row.id,row.client,row.clientName,row.address,row.request,row.notes].join(' ').toLowerCase().includes(q))).sort((a,b)=>`${a.date??''}${a.time??''}`.localeCompare(`${b.date??''}${b.time??''}`)); }
