# Dashboard feature

Read-only v7 module. Its model receives intervention, operator, team, vehicle, and message DTOs
from repositories; the view renders only into the supplied container; the feature owns its
EventBus subscriptions and removes them during `unmount()`.

Placeholder for the future independently migrated feature.
