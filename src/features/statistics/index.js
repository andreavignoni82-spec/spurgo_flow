import{createReadFeature}from'../../shared/ui/operational.js';import{statisticsReadModel}from'../../shared/selectors/operations.js';
const metric=(cls,icon,label,value,foot)=>`<article class="ops-card metric ${cls}"><span class="metric-icon">${icon}</span><div><span>${label}</span><strong>${value}</strong><small>${foot}</small></div></article>`;
export const statisticsFeature=createReadFeature('statistics','Statistiche',async c=>statisticsReadModel(await c.services.interventions.listInterventions()),m=>`<section class="metrics statistics-metrics">${metric('green','▣','Interventi',m.total,'Totale registrato')}${metric('blue','✓','Terminati',m.completed,'Completati')}${metric('red','×','Annullati',m.cancelled,'Non eseguiti')}${metric('orange','!','Urgenti',m.urgent,'Priorità alta')}${metric('purple','◷','Ore stimate',Math.round((Number(m.estimatedMinutes)||0)/60*10)/10,'Carico pianificato')}</section><section class="ops-card"><div class="table-title"><div><h2>Riepilogo operativo</h2><small>Dati calcolati dagli interventi persistiti</small></div></div><div class="stats-summary"><div><span>Tasso completamento</span><strong>${m.total?Math.round((m.completed/m.total)*100):0}%</strong></div><div><span>Incidenza urgenze</span><strong>${m.total?Math.round((m.urgent/m.total)*100):0}%</strong></div><div><span>Durata media stimata</span><strong>${m.total?Math.round((Number(m.estimatedMinutes)||0)/m.total):0} min</strong></div></div></section>`);
