const esc = value => String(value ?? '').replace(/[&<>"']/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' })[char]);
export function renderTeams({ teams, operators, form, saving, error }) {
  const selected = new Set(form?.operatorIds || []);
  return `<section class="sf-people-teams"><h2>Squadre</h2><form data-role="team-form"><h3>${form?.id ? 'Modifica squadra' : 'Nuova squadra'}</h3><label>Nome<input name="name" value="${esc(form?.name)}"></label><label>Mezzo<input name="vehicle" value="${esc(form?.vehicle)}"></label><fieldset><legend>Operatori</legend>${operators.map(operator => `<label><input type="checkbox" name="operatorIds" value="${esc(operator.id)}" ${selected.has(operator.id) ? 'checked' : ''}>${esc(`${operator.nome} ${operator.cognome}`.trim())}${operator.active ? '' : ' (disattivato)'}</label>`).join('') || '<small>Nessun operatore disponibile.</small>'}</fieldset>${error ? `<p class="sf-people-error">${esc(error)}</p>` : ''}<div class="sf-people-actions"><button ${saving ? 'disabled' : ''}>${saving ? 'Salvataggio…' : 'Salva squadra'}</button>${form?.id ? '<button type="button" data-action="cancel-team">Annulla</button>' : ''}</div></form><div class="sf-people-list">${teams.map(team => `<article data-team-id="${esc(team.id)}"><div><strong>${esc(team.name)}</strong><small>${esc(team.vehicle || 'Nessun mezzo')} · ${team.operatorIds.length} operatori</small></div><div class="sf-people-actions"><button data-action="edit-team">Modifica</button><button data-action="delete-team">Elimina</button></div></article>`).join('') || '<p>Nessuna squadra.</p>'}</div></section>`;
}
