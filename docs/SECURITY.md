# Security

Alpha.1 contains no authentication, authorization, personal data, Firebase SDK, project configuration, production endpoint, or credential. Secrets must never be committed. Future infrastructure uses environment-specific configuration, least privilege, emulator-backed tests, validation at trust boundaries, and security-rule tests before connecting to any backend.
