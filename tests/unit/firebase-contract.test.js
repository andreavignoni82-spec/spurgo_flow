import test from 'node:test'; import assert from 'node:assert/strict'; import { REPOSITORY_CONTRACTS,assertRepositoryContract } from '../../src/domain/contracts/repositories.js'; import { createFirebaseRepositories } from '../../src/infrastructure/firebase/firebase-repositories/index.js'; import { createRepositories } from '../../src/infrastructure/repositories/create-repositories.js';
const adapter={list:async()=>[],get:async()=>null,create:async(_c,_i,v)=>v,update:async(_c,i,v)=>({...v,id:i}),remove:async()=>null,query:async()=>[],queryArray:async()=>[]};
test('Firebase test adapters implement every repository contract',()=>{const repos=createFirebaseRepositories({client:{adapter}});for(const name of Object.keys(REPOSITORY_CONTRACTS))assert.equal(assertRepositoryContract(name,repos[name]),repos[name]);});
test('memory is default and unconfigured Firebase fails clearly',()=>{assert.ok(createRepositories().clients);assert.throws(()=>createRepositories({driver:'firebase'}),/without test configuration/);});
